[project]
name = "rabbitmq-consumer"
version = "0.1.0"
description = "Functional RabbitMQ consumer service"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "aiohttp>=3.9.0",
    "faststream[rabbit]>=0.6.5",
]
