[project]
name = "rabbitmq-consumer"
version = "0.1.0"
description = "Functional RabbitMQ consumer service"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "aio-pika>=9.4.0",
    "aiohttp>=3.9.0"
]
